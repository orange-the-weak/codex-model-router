interface:
  display_name: "Codex Auto Model Router"
  short_description: "基于任务证据为 GPT-5.6 自动选择模型与推理强度"
  default_prompt: "Use $codex-auto-model-router to re-evaluate this Apply request from task evidence and the bundled stale-aware benchmark prior, choose GPT-5.6 model and reasoning per segment, never fall back or restore to GPT-5.5 while any GPT-5.6 route is available, stop on failure, restore a verified GPT-5.6 original once, and record only verified usage."

interface:
  display_name: "Codex Auto Model Router"
  short_description: "快速查询模型使用比例并基于可靠历史自动微调任务分配"
  default_prompt: "Use $codex-auto-model-router to choose one deterministic route for this request, verify the current task from local Codex settings metadata, skip costly switching for tiny work, restore the original route after a real switch, and persist validated model-effort history."

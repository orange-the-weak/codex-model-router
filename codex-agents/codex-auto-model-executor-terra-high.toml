name = "codex_auto_model_executor_terra_high"
description = "Workspace-write GPT-5.6 Terra High executor for a bounded routed project segment."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Execute only the bounded segment supplied by the codex-auto-model-router coordinator. Require ROUTE_PROJECT_MODELS_EXECUTOR=1, read applicable AGENTS.md files, preserve unrelated changes, do not delegate, verify proportionately, and return changed files, checks, remaining risks, and exposed runtime model metadata. Never create a top-level Codex task.
"""

name = "project_model_router_terra_low"
description = "Read-only GPT-5.6 Terra Low router for narrow, low-risk repository analysis."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Perform only Assess or Retune model-routing analysis. Read applicable AGENTS.md files and the global route-project-models skill. Inspect representative evidence without builds, tests, edits, or recursive delegation. Return a complete Markdown-ready routing report and structured ledger payload to the parent task. Never create a top-level Codex task.
"""

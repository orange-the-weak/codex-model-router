name = "project_model_executor_low"
description = "Workspace-write GPT-5.6 Sol Low executor for a bounded routed project segment."
model = "gpt-5.6-sol"
model_reasoning_effort = "low"
sandbox_mode = "workspace-write"
developer_instructions = """
Execute only the bounded segment supplied by the route-project-models coordinator. Require ROUTE_PROJECT_MODELS_EXECUTOR=1, read applicable AGENTS.md files, preserve unrelated changes, do not delegate, verify proportionately, and return changed files, checks, remaining risks, and exposed runtime model metadata. Never create a top-level Codex task.
"""
